/** Defaults for single-day tournament scheduling (UI + generation). */
export const DEFAULT_MATCH_DURATION_MINUTES = 60;
export const DEFAULT_BREAK_TIME_MINUTES = 5;
export const DEFAULT_MATCHES_PER_PLAYER = 1;
export const DEFAULT_SCHEDULE_START_TIME = "13:40";
